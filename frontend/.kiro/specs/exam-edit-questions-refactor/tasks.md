# Implementation Plan

- [x] 1. Set up component structure and shared utilities
  - [x] 1.1 Create `src/components/questions/` directory structure with index exports
    - Create folder structure: `questions/`, `questions/skeletons/`, `questions/__tests__/`
    - Set up barrel exports in `index.ts`
    - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - [x] 1.2 Create calculation utility functions for question counts and marks
    - Implement `calculateQuestionCounts(questions)` returning main and total counts
    - Implement `calculateTotalMarks(questions)` summing all marks
    - Implement `getQuestionDisplayData(questionSet)` combining all calculations
    - _Requirements: 1.4, 1.5_
  - [ ]* 1.3 Write property test for question count calculation (Property 1)
    - **Property 1: Question count calculation accuracy**
    - **Validates: Requirements 1.4**
  - [ ]* 1.4 Write property test for marks calculation (Property 2)
    - **Property 2: Marks calculation accuracy**
    - **Validates: Requirements 1.5**

- [x] 2. Implement skeleton loading components
  - [x] 2.1 Create SubQuestionCardSkeleton component
    - Render skeleton with question text placeholder and action button placeholder
    - Apply appropriate indentation styling
    - _Requirements: 2.3_
  - [x] 2.2 Create MainQuestionCardSkeleton component
    - Render skeleton with question header, marks badge, and expandable area
    - Accept `subQuestionCount` prop for nested skeletons
    - _Requirements: 2.3_
  - [x] 2.3 Create QuestionSetCardSkeleton component
    - Render skeleton with title, question count badges, and expand toggle
    - Accept `questionCount` prop for nested MainQuestionCardSkeletons
    - _Requirements: 2.2_
  - [x] 2.4 Create QuestionSetListSkeleton component
    - Accept `count` prop (default 3) for number of QuestionSetCardSkeletons
    - Apply animated pulse effect to all skeleton elements
    - _Requirements: 2.1_
  - [ ]* 2.5 Write property test for skeleton count rendering (Property 3)
    - **Property 3: Skeleton count matches expected structure**
    - **Validates: Requirements 2.1, 2.3**

- [x] 3. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 4. Implement QuestionActions shared component
  - [x] 4.1 Create QuestionActions dropdown component
    - Implement dropdown with View, Edit, Delete actions
    - Conditionally show "Add Sub-Question" for main questions
    - Handle loading state with spinner on affected action
    - _Requirements: 4.5, 6.4_
  - [ ]* 4.2 Write unit tests for QuestionActions component
    - Test rendering with different questionType values
    - Test action callbacks are invoked correctly
    - Test loading state disables interactions
    - _Requirements: 4.5_

- [x] 5. Implement SubQuestionCard component
  - [x] 5.1 Create SubQuestionCard component
    - Display question number, text (EditorJS rendered), and marks badge
    - Include QuestionActions dropdown
    - Show answer indicator (warning if none, success with count if present)
    - Apply hover highlight effect
    - _Requirements: 4.3, 6.1, 6.2, 6.3_
  - [ ]* 5.2 Write property test for answer indicator accuracy (Property 8)
    - **Property 8: Answer indicator accuracy**
    - **Validates: Requirements 6.2, 6.3**

- [x] 6. Implement MainQuestionCard component
  - [x] 6.1 Create MainQuestionCard component
    - Display question number, text, marks, and expand/collapse toggle
    - Render sub-questions when expanded using SubQuestionCard
    - Include QuestionActions dropdown with "Add Sub-Question" option
    - Show answer indicator and sub-question count badge
    - _Requirements: 4.2, 5.2, 6.1, 6.2, 6.3_
  - [ ]* 6.2 Write property test for component type matching hierarchy (Property 6)
    - **Property 6: Component type matches question hierarchy**
    - **Validates: Requirements 4.2, 4.3**
  - [ ]* 6.3 Write property test for expand toggle behavior (Property 7)
    - **Property 7: Expand toggle inverts state**
    - **Validates: Requirements 5.1, 5.2**

- [x] 7. Implement QuestionSetCard component
  - [x] 7.1 Create QuestionSetCard component
    - Display title, question counts (main/total), total marks
    - Implement expand/collapse with summary when collapsed
    - Render MainQuestionCards when expanded
    - Include edit/delete actions for the question set
    - _Requirements: 1.1, 1.2, 1.4, 1.5, 4.1, 5.1, 5.3_
  - [ ]* 7.2 Write unit tests for QuestionSetCard component
    - Test collapsed state shows summary
    - Test expanded state shows questions
    - Test action callbacks
    - _Requirements: 4.1, 5.1, 5.3_

- [x] 8. Implement QuestionSetList container component
  - [x] 8.1 Create QuestionSetList component
    - Accept questionSets array and loading state
    - Render QuestionSetListSkeleton when loading
    - Render QuestionSetCards when loaded
    - Handle empty state with helpful message
    - _Requirements: 2.1, 2.4, 4.1_
  - [ ]* 8.2 Write unit tests for QuestionSetList component
    - Test loading state renders skeletons
    - Test loaded state renders cards
    - Test empty state message
    - _Requirements: 2.1, 2.4_

- [x] 9. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 10. Implement QuestionDialog component
  - [x] 10.1 Create QuestionDialog component
    - Accept mode prop ('add-main', 'add-sub', 'edit')
    - Render question form with appropriate initial state based on mode
    - Handle form submission with API calls
    - Implement cancel behavior with state reset
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - [ ]* 10.2 Write property test for dialog mode form state (Property 4)
    - **Property 4: Dialog mode determines form state**
    - **Validates: Requirements 3.1, 3.2, 3.3**
  - [ ]* 10.3 Write property test for dialog cancel behavior (Property 5)
    - **Property 5: Dialog cancel resets state**
    - **Validates: Requirements 3.5**

- [x] 11. Integrate components into exam paper edit page
  - [x] 11.1 Refactor edit page to use new QuestionSetList component
    - Replace existing hierarchical questions rendering with QuestionSetList
    - Wire up all action handlers (edit, delete, add question, add sub-question)
    - Connect QuestionDialog for add/edit operations
    - _Requirements: 1.1, 1.2, 1.3, 3.1, 3.2, 3.3_
  - [x] 11.2 Implement responsive layout adjustments
    - Apply desktop layout with side-by-side metadata
    - Apply tablet layout with stacked metadata
    - Apply mobile layout with collapsed non-essential info
    - Ensure dialog displays as full-screen modal on mobile
    - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - [ ]* 11.3 Write integration tests for edit page question management
    - Test loading states display correctly
    - Test CRUD operations work end-to-end
    - Test responsive behavior
    - _Requirements: 1.1, 2.1, 3.4_

- [x] 12. Final Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
